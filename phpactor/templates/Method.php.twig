{# Method.php.twig #}
{% if prototype.docblock.notNone %}
/**
{% for line in prototype.docblock.asLines %}
 * {{ line|raw }}
{% endfor %}
 */
{% endif %}
{% if prototype.isAbstract %}abstract {%endif %}{{ prototype.visibility }} {% if prototype.isStatic %}static {%endif %}function {{ prototype.name}}({% spaceless %}
{% for parameter in prototype.parameters %}{{ generator.render(parameter, variant)|raw }}{% if not loop.last %}, {% endif %}{% endfor %}{% endspaceless %})
{% if prototype.isStatic %}
{% if prototype.docblock.notNone %}
/**
 * @var {{ prototype.returnType.type|default('mixed') }}
 */
{% endif %}
{% endif %}
